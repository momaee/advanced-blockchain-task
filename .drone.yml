kind: pipeline
name: default
type: docker

steps:
  - name: test
    image: golang:1.16
    commands:
      - echo ${TEST_ENV}
      - echo ${DRONE_GITHUB_CLIENT_ID}
      - echo ${DRONE_SERVER_PROTO}
